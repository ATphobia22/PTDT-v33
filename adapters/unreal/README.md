# PTDT Unreal Adapter

Optional runtime adapter for Unreal Engine. It consumes validated SceneState-derived manifests and preserves provenance. Unreal installation is not required for core CI; adapter contracts are validated keylessly.
