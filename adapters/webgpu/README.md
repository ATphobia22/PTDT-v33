# PTDT WebGPU Adapter

Consumes validated derived scene/tile manifests for interactive GPU rendering. WebGPU is a presentation/runtime layer and must not mutate authoritative SceneState.

Shader validation is performed in CI without paid hosted graphics services.
