# PTDT Unity Adapter

Optional runtime adapter for Unity. It consumes SceneState-derived manifests and never owns authoritative spatial/evidence state. Unity installation is not required for core CI; adapter contracts are validated keylessly.
