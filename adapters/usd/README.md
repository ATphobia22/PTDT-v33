# PTDT OpenUSD Adapter

OpenUSD is the interchange/render/simulation representation for Houdini/Solaris, Unreal, Unity tooling, and offline rendering. It never becomes the authoritative engineering database.

Derived USD assets must preserve source identifiers, transforms, CRS/vertical-datum metadata, and evidence hashes.
