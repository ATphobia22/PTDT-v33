# PTDT MVT Adapter

Derives Mapbox Vector Tile layers from authoritative PTDT spatial state. MVT is a delivery representation, not an authority source.

Requirements: explicit CRS, vertical datum, provenance, deterministic tile hash. Core runtime is keyless and compatible with self-hosted MapLibre/MVT infrastructure.
