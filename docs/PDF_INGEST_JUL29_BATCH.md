# PDF ingest decisions — 2026-07-29 batch

Truth filter applied to uploaded specs. **Keep** engineering/UI patterns that match the repo; **reject** regulatory overclaim and non-physical “quantum” telemetry.

| Document | Keep | Reject / demote |
|----------|------|------------------|
| **Blender Vector Overlay & Shader** (VSC5b) | bpy extrude/bevel/cyclic patterns; cyan/green emission colors as **offline viz** recipe | “Visual proof” of HEC-RAS hazards; gauge_height > 47 as auto legal trigger |
| **Family Engineering System Will Valuation** (bn42V) | NAVD 88 mandate; clearance math form LAG−BFE; Manning equation with n=0.045, S=0.00015; velocity table as **screening** | Claims that platform alone “proves natural exemption,” eliminates insurance, or that LAG/FFE are already “Verified/As-Built” without PE survey; estate valuation as engineering fact |
| **React query / R3F water mesh** (KZCUT) | Optional `useMutation` + fetch pattern; R3F plane water as UI demo | Hard dependency on `/api/v1/twin/simulation` governance decision as PE output |
| **GeospatialTerrainProcessor** | Fallback LAG 377.2 / BFE 375.0 / NAVD88 when no GeoTIFF; optional rasterio | Treating fallback as survey-grade |
| **QEC (stim/pymatching)** (AbbS3) | — | **Reject** surface-code decoding of USGS as signal integrity for hydrology |
| **QNPR (Qiskit riprap)** (HLr7m) | — | **Reject** quantum riprap D50 / mocked −17.52 kcal/mol as engineering design |
| **Zustand + Zod** (fvNs0) | Optional client store + schema shapes for stage/discharge | `governance.decision` / crypto hash as agency approval |
| **HUD App** (yNfSh) | Layout patterns (header, stage inputs, solver button) | Boot claiming “Zero-trust confirmed” or auto ONLINE certification |
| **UI dashboard MapLibre shell** (KGOjB) | MapLibre + layer toggles + proxy `/api` → :8000 | Daubert claims in UI copy |
| **Archimedes sovereign core** (Iip7C) | HydraulicState dataclasses; USGS NWIS fetch; EnKF-style blend as **optional screen**; ReportLab drafts | OIDC theater as security product; OpenMI adapters that only increment fake depth; auto `TriStateGovernanceState.decision` as No-Rise |

## Elevation numbers (hypothesis until PE survey)

| Parameter | Value in PDFs | Repo stance |
|-----------|---------------|-------------|
| BFE | 375.0 ft NAVD 88 | Hypothesis / FIS context — confirm on effective map |
| LAG | 377.2 ft | Hypothesis — needs sealed survey |
| FFE | 382.5 ft | Hypothesis — needs sealed survey |
| Clearance | +2.2 ft LAG−BFE | Arithmetic only; not LOMA until filed |

## Manning screening (aligned with valuation PDF table)

\( V = (1.486/n)\, R_h^{2/3} S^{1/2} \) with \( n = 0.045 \), \( S = 0.00015 \), \( R_h \approx d \) for wide floodplain screen only — **not** substitute for HEC-RAS.
