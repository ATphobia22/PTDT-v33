# Visualization notes

## Preferred (no paid token)

- **MapLibre GL** + open terrain/vector sources already targeted in the frontend stack
- Optional local PMTiles / 3DEP-derived surfaces

## Cesium HTML draft

- Requires **Cesium Ion** access token for many terrain/3D assets
- Coordinates in one draft (`37.8459, -88.0051`) may not match survey coords used elsewhere (`37.9035, -88.0007`) — **verify before any demo**
- Flood plane extrusion is a **toy visual**, not a regulatory inundation map

## Godot “G1P-VERIFIED” overlay

- SHA of UI state is **not** a submission artifact
- Do not present movie-render hashes as PE or agency evidence
